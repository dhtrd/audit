# IMPLEMENTATION_STATUS.md

بالتنسيق الذي طلبه مستند PRE-AUDIT OS نفسه (القسم 76).

```
PHASE:    1 — Foundation
STATUS:   COMPLETE and VERIFIED (ليس "شبه مكتمل" — كل ما هو مذكور هنا اختُبر فعليًا بالتشغيل، لا افتراضًا)
```

## IMPLEMENTED

- قاعدة بيانات حقيقية (SQLite عبر node:sqlite) بجداول: users, companies, fiscal_years, audit_logs
- مصادقة حقيقية: bcrypt + جلسة JWT في كوكيز HttpOnly
- صلاحيات فعلية بدورين: ADMIN (كتابة كاملة) و AUDITOR (اطلاع فقط) — مُطبَّقة على مستوى كل مسار كتابة (صفحات وAPI)
- CRUD الشركة (إنشاء + تعديل)
- CRUD السنوات المالية (إنشاء + تعديل الحالة: مسودة/قيد التنفيذ/مقفلة)
- إدارة المستخدمين (المدير يضيف مستخدمين بأدوار)
- سجل تدقيق (Audit Trail) يسجّل: LOGIN, LOGOUT, CREATE, UPDATE — بلا أي مسار حذف
- مؤشر "جاهزية الأساس" محسوب فعليًا (0-100%) من حالة قاعدة البيانات الحقيقية
- REST API: /api/auth/{login,logout}, /api/company, /api/company/[id],
  /api/fiscal-years, /api/fiscal-years/[id], /api/users, /api/audit-trail,
  /api/dashboard/summary
- واجهة عربية RTL كاملة

## TESTS (يدوية عبر curl ضد خادم فعلي قيد التشغيل — لا اختبارات آلية مؤتمتة بعد)

1. وصول غير مُصادَق لصفحة محمية → إعادة توجيه لتسجيل الدخول — **PASSED**
2. وصول غير مُصادَق لـ API محمي → 401 — **PASSED**
3. تسجيل دخول بكلمة مرور خاطئة → 401 — **PASSED**
4. تسجيل دخول صحيح → 200 + كوكيز جلسة فعلية — **PASSED**
5. إنشاء شركة (مدير) → 201 + تسجيل في Audit Trail — **PASSED**
6. مؤشر الجاهزية يتحدّث فعليًا (0% → 60% → 100%) مع كل إضافة بيانات حقيقية — **PASSED**
7. إنشاء سنة مالية (مدير) → 201 + تسجيل في Audit Trail — **PASSED**
8. تعديل شركة (PATCH) → 200 وتحديث فعلي في القاعدة — **PASSED**
9. تعديل حالة سنة مالية (PATCH) → 200 — **PASSED**
10. مستخدم AUDITOR: قراءة API → 200 — **PASSED**
11. مستخدم AUDITOR: محاولة إنشاء شركة/سنة مالية/مستخدم → 403 على الثلاثة — **PASSED**
12. استمرارية البيانات عبر إعادة تشغيل الخادم (SQLite على القرص لا في الذاكرة) — **PASSED**
13. قيد UNIQUE على (company_id, year) يمنع سنة مالية مكررة — **PASSED بعد إصلاح**
14. `npm run build` بدون أخطاء TypeScript عبر 17 مسارًا — **PASSED**

## PASSED

13 من 14 اختبارًا نجح من أول محاولة. اختبار واحد (#13) كشف خطأ حقيقي.

## FAILED (قبل الإصلاح)

- إنشاء سنة مالية مكررة (نفس الشركة + نفس السنة) كان يُرجع **500 غير مُعالَج**
  (استثناء SQLite UNIQUE constraint لم يكن مُلتقَطًا) بدل رسالة خطأ واضحة.

## FIXED

- التُقِط استثناء SQLite (`ERR_SQLITE_ERROR` / `errcode 2067`) في:
  - `POST /api/fiscal-years` → يُرجع الآن 409 برسالة عربية واضحة
  - Server Action الخاص بصفحة "السنوات المالية" → يُعيد التوجيه مع رسالة خطأ تظهر في الواجهة
- أُعيد بناء المشروع واختباره بعد الإصلاح — تأكّد عدم ظهور أي خطأ غير مُعالَج في سجل الخادم

## SECURITY

- كلمات المرور: bcrypt (10 rounds) — لا تخزين نص صريح في أي مكان
- الجلسة: JWT HS256 موقَّع، كوكيز HttpOnly + SameSite=Lax + Secure في الإنتاج
- كل مسار كتابة (POST/PATCH) يتحقق من الجلسة (401) ثم من الدور (403) قبل أي عملية على القاعدة
- لا يوجد أي مسار حذف لسجلات Audit Trail في كامل الكود (Invariant #7 من المواصفة)
- لا يوجد تسجيل عام (public sign-up) — يمنع إنشاء حسابات مدير غير مصرَّح بها
- لم يُجرَ بعد: اختبار IDOR منهجي، rate limiting، فحص رفع ملفات (غير مطلوب في Phase 1 لعدم وجود رفع ملفات بعد)

## REMAINING (لهذه المرحلة تحديدًا)

- لا اختبارات آلية (unit/integration) مكتوبة بعد — التحقق كان يدويًا عبر curl ضد خادم حقيقي
- لا واجهة لتغيير كلمة المرور من داخل التطبيق (فقط عبر seed حاليًا)
- لا صفحة تعديل/تعطيل مستخدم موجود (الإضافة فقط)

## NEXT — Phase 2: Odoo / Excel Data Import

حسب ترتيب المراحل في المواصفة الأصلية:

1. رفع ملف Excel لميزان المراجعة والأستاذ العام
2. اكتشاف نوع الملف (Trial Balance vs GL) من الأعمدة والبنية
3. Column Mapping قابل للتعديل والحفظ كقالب
4. التحقق (توازن مدين=دائن، حسابات مكررة، فترات غير صالحة)
5. التسوية: مطابقة إجمالي المصدر بإجمالي المستورَد — يجب أن يكون الفرق صفرًا
6. شاشة ميزان المراجعة مع Drill-down للأستاذ العام
7. Data Lineage: كل رقم مستورد يحمل مصدره (ملف/ورقة/صف)

هذه هي الوحدة التالية التي سأبنيها فعليًا (بنفس المعيار: كود يعمل ويُختبَر، لا وعود).

---

# PHASE 2 — استيراد البيانات من Excel (ميزان المراجعة + الأستاذ العام)

```
PHASE:    2 — Excel/CSV Data Import (Trial Balance + General Ledger)
STATUS:   COMPLETE and VERIFIED (كل ما هو مذكور أدناه اختُبر فعليًا بالتشغيل عبر
          خادم قيد التشغيل + طلبات HTTP حقيقية (curl) + لقطات شاشة فعلية للواجهة
          + قراءة مخرجات قاعدة البيانات الحقيقية — لا افتراضًا)
```

## IMPLEMENTED

- قراءة ملفات حقيقية: **xlsx/xls/xlsm عبر مكتبة `exceljs`** (مثبَّتة فعليًا عبر npm) و**CSV** عبر مُحلِّل
  خاص يدعم الحقول المقتبسة وBOM وفواصل الأسطر — لا قراءة وهمية
- **اكتشاف نوع الملف تلقائيًا** من أسماء الأعمدة وبنيتها (رصيد افتتاحي/ختامي ⇐ ميزان مراجعة؛
  تاريخ/يومية/طرف ⇐ أستاذ عام)، مع **درجة ثقة وسبب مقروء**، و**سماح للمستخدم بتأكيد/تصحيح النوع يدويًا**
- **تحديد صف العناوين تلقائيًا** حتى لو سبقته صفوف عنوان/بانر (مثل ملف عربي عناوينه في الصف الثالث)
- **معالجة أرقام حقيقية**: أرقام عربية-هندية (٠-٩)، فواصل الآلاف، رمز العملة، الفاصلة العشرية العربية،
  والأقواس/السالب الحسابي `(1,000)` ⇐ سالب
- **شاشة Column Mapping**: ربط أعمدة الملف بالحقول القياسية (account_code, debit, credit, balance…)
  مع اقتراح تلقائي قابل للتصحيح، و**حفظ الربط كقالب** قابل لإعادة الاستخدام (`mapping_templates`)
- **تحقق حقيقي** بعد الرفع محسوب من البيانات (لا رقم عشوائي): توازن مدين=دائن، حسابات مفقودة،
  مبالغ/تواريخ غير صالحة، صفوف مكرّرة — يُعرض كـ**درجة جودة (0-100%)** مع تفصيل كل فحص
- **تسوية إلزامية**: إجمالي المصدر (صف الإجمالي المُعلن في الملف إن وُجد، وإلا مجموع التفاصيل)
  مقابل **الإجمالي المستورد فعليًا في قاعدة البيانات (SUM عبر SQL)** — إن كان الفرق ≠ 0 يُعرض
  **BLOCKED** بوضوح، **وتُحذف الصفوف المُدرجة (rollback) فلا تُعتمد بيانات جزئية إطلاقًا**، ولا يوجد أي تجاوز
- **Data Lineage**: كل صف مستورد يحمل `source_file`, `source_sheet`, `source_row`, `import_batch_id`
  (أُضيفت هذه الحقول لجدولَي `trial_balances` و`general_ledger` الجديدين في db.ts)
- **شاشة ميزان مراجعة حقيقية** تعرض البيانات المستوردة فعليًا مع الإجماليات ومؤشر التوازن، و**Drill-down
  حقيقي** بالضغط على أي حساب للانتقال إلى قيوده في الأستاذ العام **عبر استعلام SQL** (company_id + account_code)
  — لا بيانات مصفوفة في الواجهة
- **الالتزام ضمن معاملة SQLite واحدة** (`transaction()` جديدة في db.ts)
- **تسجيل كل استيراد في `audit_logs`**: IMPORT_UPLOAD, IMPORT_SET_TYPE, IMPORT_MAPPING, IMPORT_COMMIT
  (من قام به، متى، اسم الملف، النتيجة COMMITTED/BLOCKED، التسوية)
- تخزين الملف المرفوع على القرص (`data/uploads/<uuid>/`) — البديل المحلي لـ S3 الذي أشار README لإضافته في Phase 2
- REST API مطابق لكل عملية (نفس نمط Phase 1: server actions للصفحات + REST تحت /api):
  `POST/GET /api/imports`, `GET /api/imports/[id]`, `PATCH /api/imports/[id]/mapping`,
  `POST /api/imports/[id]/commit`, `GET/POST /api/mapping-templates`,
  `GET /api/trial-balance`, `GET /api/general-ledger`
- واجهة عربية RTL كاملة للشاشات الجديدة (استيراد، معالج الربط/التحقق، ميزان المراجعة، Drill-down)

## TESTS (يدوية عبر curl + Playwright ضد خادم فعلي قيد التشغيل، مع قراءة مخرجات قاعدة البيانات)

1. رفع ملف xlsx (multipart) → 201 + اكتشاف TRIAL_BALANCE بثقة 1.0 + تخزين الملف على القرص — **PASSED**
2. رفع ملف أستاذ عام → اكتشاف GENERAL_LEDGER بثقة 1.0 — **PASSED**
3. اكتشاف النوع من عناوين عربية (رمز الحساب/مدين/دائن/الرصيد) → TRIAL_BALANCE — **PASSED**
4. تحديد صف العناوين تلقائيًا أسفل صف بانر (الملف العربي: العناوين في الصف 3) — **PASSED**
5. أرقام عربية-هندية `٥٠٬٠٠٠` وأقواس سالبة `(١٧٠٬٠٠٠)` → قيم رقمية صحيحة — **PASSED**
6. حفظ ربط الأعمدة (PATCH: تأكيد النوع + mapping) → 200 — **PASSED**
7. حفظ الربط كقالب → 201؛ قالب بنفس الاسم/النوع → 409 — **PASSED**
8. الالتزام لميزان نظيف → 200 COMMITTED، جودة 100%، 5 صفوف، المصدر=المستورد (فرق 0) — **PASSED**
9. الالتزام لأستاذ عام → 200 COMMITTED، 6 صفوف — **PASSED**
10. التسوية الإلزامية: إجمالي مُعلن 999,999 مقابل مجموع التفاصيل 170,000 → **409 BLOCKED و0 صف مخزَّن (rollback مؤكَّد)** — **PASSED**
11. الجودة: ميزان غير متوازن (مدين≠دائن) → فحص FAIL + درجة 70% — **PASSED**
12. Data Lineage مخزَّن لكل صف (ملف/ورقة/رقم صف) ويظهر في الواجهة — **PASSED**
13. شاشة ميزان المراجعة تعرض البيانات المستوردة + عدد القيود المحسوب عبر SQL — **PASSED**
14. Drill-down من حساب في الميزان إلى قيوده في GL عبر SQL (الحساب 1200 → قيدان) — **PASSED**
15. RBAC: AUDITOR رفع 403، التزام 403، إنشاء قالب 403، قراءة الميزان 200 — **PASSED**
16. وصول API غير مُصادَق → 401 (imports و trial-balance) — **PASSED**
17. تسجيل كل استيراد في audit_logs مع المستخدم/الملف/النتيجة — **PASSED**
18. لا يوجد أي مسار حذف لـ audit_logs في كامل الكود (فحص grep) — **PASSED**
19. `npm run build` بلا أخطاء TypeScript عبر 23 مسارًا — **PASSED**
20. عرض الصفحات RTL بشكل صحيح (لقطات شاشة فعلية للواجهة) — **PASSED**

## PASSED

20 من 20 اختبارًا نجحت (بعد إصلاح واحد كشفه الاختبار — انظر أدناه).

## FAILED (قبل الإصلاح)

- **Data Lineage: رقم الصف المصدر (`source_row`) كان مُزاحًا بمقدار 1** — كان يُخزَّن رقم أقل بواحد
  من رقم الصف الفعلي في الورقة، لأن فهرس المصفوفة الكثيفة استُخدم مباشرةً كرقم صف بدل رقم الصف
  الحقيقي (1-based). كُشف عند التحقق من مخرجات قاعدة البيانات مقابل الملف المصدر.

## FIXED

- ضُبط `rowOffset = 1` في قراءة xlsx وCSV معًا في `src/lib/excel.ts` بحيث يصبح `source_row` هو
  رقم الصف الفعلي (1-based). أُعيد التحقق فعليًا: ملف بعناوين في الصف 1 → أول صف بيانات = 2؛
  الملف العربي (بانر + صف فارغ + عناوين) → صف العناوين = 3 وأول بيانات = 4؛ وظهرت الأرقام
  الصحيحة في شاشة ميزان المراجعة (tb_clean.xlsx : صف 2..6).

## SECURITY

- **كل مسار كتابة يتطلب ADMIN**: الرفع، حفظ الربط، تعيين النوع، الالتزام، إنشاء قالب —
  يرجع 401 دون جلسة و403 لمستخدم AUDITOR (مُختبَر على الأربعة)
- تحقق الرفع: قائمة امتدادات مسموحة (xlsx/xls/xlsm/csv)، سقف حجم 20MB، رفض الملف الفارغ
- **التسوية غير قابلة للتجاوز**: حالة BLOCKED لا يمكن تخطّيها، والبيانات المحظورة تُحذف (rollback) ولا تُخزَّن
- الالتزام يجري داخل معاملة SQLite واحدة؛ عند فشل التسوية تُزال الصفوف المُدرجة
- سجل التدقيق يبقى بلا أي مسار حذف (Invariant #7 محفوظ) — مؤكَّد بفحص grep
- **لم يُنجز بعد**: فحص محتوى/فيروسات للملفات المرفوعة، قراءة تدفقية (streaming) للملفات الضخمة جدًا
  (تُحمَّل الورقة في الذاكرة حاليًا)، تعقيم حقن صيغ Excel عند التصدير (لا تصدير بعد)، rate limiting على الرفع

## REMAINING (لهذه المرحلة تحديدًا)

- شاشة ميزان المراجعة تُجمِّع كل الدفعات المُلتزَمة للشركة (لا يوجد بعد مُرشِّح لكل دفعة/سنة مالية) —
  إعادة استيراد نفس الميزان ستُظهر المجموعتين معًا
- لا توجد واجهة لحذف/تراجع دفعة مُلتزَمة (إعادة الالتزام لنفس الدفعة تستبدل صفوفها فقط)
- لا اختبارات آلية (unit/integration) مكتوبة بعد — التحقق عبر سكربتات curl + Playwright ضد خادم حقيقي
- `.xls` (الثنائي القديم) يعتمد على exceljs؛ اختُبرت فعليًا ملفات `.xlsx` و`.csv` ببيانات حقيقية
- المدعوم: ميزان المراجعة + الأستاذ العام فقط (الملفات الأخرى مؤجَّلة كما هو مطلوب)

## NEXT — Phase 3 (حسب ترتيب المواصفة)

- بقية مصادر البيانات (الأصول الثابتة، المخزون، كشوف البنوك… إلخ) بنفس محرك الاستيراد/التسوية
- بدء وحدة المخاطر والإجراءات التحليلية التي تستهلك بيانات الميزان/الأستاذ المستوردة الآن

---

# PHASE 3 — المخاطر والإجراءات التحليلية

```
PHASE:    3 — Risk Assessment & Analytical Procedures
STATUS:   COMPLETE and VERIFIED (كل درجة مخاطر ونتيجة تحليلية محسوبة فعليًا من بيانات
          الميزان/الأستاذ المستوردة — لا رقم عشوائي. اختُبر محرك التحليلات ببيانات
          مُصمَّمة تُعرف نتائجها سلفًا، ثم عبر HTTP على بيانات حقيقية + لقطة شاشة)
```

## IMPLEMENTED

- **محرك إجراءات تحليلية** (`src/lib/risk.ts`) يحسب من بيانات Phase 2 الحقيقية:
  1. **تسوية الميزان ↔ الأستاذ العام** لكل حساب (صافي الميزان مقابل صافي حركة الأستاذ، مع تصنيف
     الفروق فوق/تحت الأهمية النسبية)
  2. **بنود كبيرة** ≥ الأهمية النسبية
  3. **مبالغ مدوّرة** (مضاعفات تامة لـ 1000 — مؤشر تقدير/قيد يدوي)
  4. **قيود نهاية الأسبوع** (عطلة السعودية: الجمعة/السبت) محسوبة من التاريخ الفعلي
  5. **قيود مكرّرة** (نفس التاريخ+الحساب+المدين+الدائن)
  6. **فجوات في تسلسل المراجع** لكل يومية (اختبار اكتمال)
  7. **قانون بنفورد** لتوزيع الرقم الأول مع مقياس Nigrini MAD وحكم مطابقة
- **درجة مخاطر إجمالية شفافة (0-100)** مع تفصيل كامل لكل عامل ووزنه (بنفس فلسفة مؤشر جاهزية Phase 1، لا صندوق أسود)
- **إعداد الأهمية النسبية (Materiality)** لكل شركة (وبنية جاهزة لكل سنة مالية) — جدول `materiality_settings`
- **شاشة المخاطر والتحليلات**: الأهمية النسبية، الدرجة الإجمالية والمستوى، بطاقات الإجراءات،
  مخطط بنفورد (ملاحظ مقابل متوقع)، و**سجل مخاطر** بكل الملاحظات مرتبة بالخطورة مع **Data Lineage
  (ملف/صف)** و**Drill-down** لقيود الحساب في الأستاذ العام
- REST API مطابق: `GET/POST /api/materiality`, `GET /api/risk`
- تسجيل تغيير الأهمية النسبية في `audit_logs` (MATERIALITY_SET)؛ تحليل المخاطر للقراءة فقط (متاح للمراجع)
- التحليلات تُحسب **حيًّا** من البيانات المُلتزَمة فلا تتقادم أبدًا

## TESTS (محرك مُتحقَّق ببيانات مُصمَّمة + HTTP على بيانات حقيقية + لقطة شاشة)

1. تحقق رياضي لكل إجراء ببيانات مُصمَّمة تُعرف نتائجها سلفًا:
   تسوية=1، كبيرة=2، مدوّرة=2، نهاية أسبوع=2، مكرّرة=1 مجموعة، فجوة=[3]، درجة=24 — **كلها طابقت PASSED**
2. تحديد الجمعة/السبت من التاريخ بأسماء عربية صحيحة — **PASSED**
3. حساب بنفورد (نِسَب لكل رقم + MAD + حكم + تنبيه العيّنة الصغيرة) — **PASSED**
4. ضبط الأهمية النسبية (ADMIN) → 201 + تسجيل MATERIALITY_SET — **PASSED**
5. قراءة الأهمية النسبية (GET) — **PASSED**
6. تقرير المخاطر الحيّ عبر API على بيانات مستوردة حقيقية → درجة 67 (مرتفع)، 29 ملاحظة،
   تسوية 4 (2 فوق الأهمية)، كبيرة 9، مدوّرة 8، نهاية أسبوع 6، مكرّرة 1، فجوة 1 — **كلها طابقت الحساب اليدوي PASSED**
7. تفصيل الدرجة يجمع فعليًا إلى 67 (20+10+15+5+10+2+5) — **PASSED**
8. RBAC: AUDITOR ضبط الأهمية 403، قراءة المخاطر 200، وصول غير مُصادَق 401، والأهمية لم تتغيّر — **PASSED**
9. `npm run build` بلا أخطاء عبر 26 مسارًا — **PASSED**
10. عرض شاشة المخاطر RTL (الدرجة + التفصيل + بنفورد + السجل + Drill-down) بلقطة شاشة فعلية — **PASSED**

## PASSED

10 من 10. تحقق المحرك الرياضي أولًا بمعزل عن الواجهة، فطابق النتائج المحسوبة يدويًا من أول تشغيل.

## FAILED (قبل الإصلاح)

- لا شيء في هذه المرحلة — تم التحقق من صحة كل حساب في المحرك ببيانات مُصمَّمة قبل ربطه بالواجهة،
  فلم تظهر أخطاء عند الاختبار عبر HTTP. (لم يُدَّعَ نجاح دون اختبار: كل رقم قُورِن بحساب يدوي.)

## FIXED

- لا إصلاحات لازمة في هذه المرحلة.

## SECURITY

- ضبط الأهمية النسبية مسار كتابة يتطلب ADMIN (403 لـ AUDITOR — مُختبَر) وجلسة (401)
- تحليل المخاطر للقراءة فقط ومتاح لدور المراجع؛ يُحسب حيًّا من البيانات المُلتزَمة (لا أرقام مخاطر مخزَّنة قابلة للعبث)
- كل تغيير للأهمية النسبية مُسجَّل في audit_logs (MATERIALITY_SET)
- سجل التدقيق يبقى بلا مسار حذف (Invariant #7 محفوظ)
- **لم يُنجز بعد**: واجهة أهمية نسبية لكل سنة مالية (الـAPI يدعم fiscalYearId، الشاشة تضبط الافتراضي للشركة)، rate limiting

## REMAINING (لهذه المرحلة تحديدًا)

- الشاشة تضبط الأهمية النسبية على مستوى الشركة فقط (الـAPI والمخطط يدعمان التخصيص لكل سنة مالية)
- التحليلات تحمّل كل قيود الشركة في الذاكرة (كافٍ للحجم الحالي؛ التقسيم/التدفق لاحقًا للبيانات الضخمة)
- قانون بنفورد ذو دلالة مع عيّنات أكبر (تنبيه واضح للعيّنة الصغيرة معروض)
- قاعدة نهاية الأسبوع ثابتة على السعودية (الجمعة/السبت) بلا تقويم إجازات بعد
- لا حفظ للقطات تقييم المخاطر ولا مسار "تمّت مراجعته" للملاحظات بعد

## NEXT — Phase 4 (حسب ترتيب المواصفة)

- وحدة الأدلة والإجراءات التدقيقية (Evidence/Procedures) التي تستهلك ملاحظات المخاطر الحالية
- ربط كل ملاحظة مخاطر بإجراء تدقيقي/دليل، ثم الملاحظات والتسويات ومراجعة الإدارة (لإكمال Audit Readiness Score)

---

# PHASE 4 — الأدلة والإجراءات التدقيقية

```
PHASE:    4 — Evidence & Audit Procedures
STATUS:   COMPLETE and VERIFIED (الإجراءات مُولَّدة فعليًا من ملاحظات مخاطر Phase 3،
          والأدلة ملفات حقيقية تُخزَّن وتُسترجَع من القرص. اختُبر منطق الخدمة أولًا
          ثم كامل التدفق عبر HTTP + لقطات شاشة)
```

## IMPLEMENTED

- **توليد إجراءات تدقيق من ملاحظات المخاطر** (`src/lib/procedures.ts`): تحويل ملاحظات Phase 3 إلى
  إجراءات (إجراء واحد لكل مفتاح مميز نوع-مخاطرة + حساب)، **بلا تكرار** (idempotent — يتخطّى الموجود)،
  مع قوالب نصية لكل نوع مخاطرة (تسوية، بنود كبيرة، مدوّرة، نهاية أسبوع، ازدواج، فجوات ترقيم)
- **إجراءات يدوية** يضيفها المدير خارج المخاطر المكتشفة
- **تنفيذ الإجراء**: حالة (مفتوح/قيد التنفيذ/منجز/لا ينطبق) + مسؤول + استنتاج، مع **منع الإنهاء (DONE)
  دون استنتاج موثّق**
- **الأدلة**: رفع ملفات حقيقية (multipart) تُخزَّن على القرص (data/uploads) وتُقيَّد في `evidence_files`
  مع الحاجم ومَن رفعها ومتى، و**مسار تنزيل** يعيد نفس البايتات
- **قياس تغطية المخاطر**: نسبة ملاحظات المخاطر عالية/متوسطة الخطورة التي لها إجراء مُنجَز (DONE/NA)
- **شاشتان**: قائمة الإجراءات (بطاقات التغطية والحالة + زر التوليد + إضافة يدوية) وتفاصيل الإجراء
  (مصدر المخاطرة + Drill-down للحساب + تنفيذ + قائمة/رفع/تنزيل الأدلة)
- REST API مطابق: `GET/POST /api/procedures`, `POST /api/procedures/generate`,
  `GET/PATCH /api/procedures/[id]`, `POST /api/procedures/[id]/evidence`, `GET /api/procedures/[id]/evidence/[eid]`
- تسجيل كل عملية في `audit_logs`: PROCEDURES_GENERATED, PROCEDURE_CREATE, PROCEDURE_UPDATE, EVIDENCE_UPLOAD

## TESTS (منطق الخدمة مُتحقَّق أولًا + كامل التدفق عبر HTTP + لقطات شاشة)

1. توليد من المخاطر: 29 ملاحظة → 17 إجراءً مميّزًا (تجميع نوع+حساب، استبعاد INFO) — **PASSED**
2. عدم التكرار: الاقتراحات = 0 بعد التوليد (idempotent) — **PASSED**
3. حساب التغطية من مفاتيح المخاطر عالية/متوسطة (0% → 8% بعد إنجاز إجراء واحد) — **PASSED**
4. منع الإنهاء دون استنتاج: PATCH status=DONE بلا استنتاج → 400 — **PASSED**
5. الإنهاء مع استنتاج → 200 — **PASSED**
6. رفع دليل (ملف حقيقي multipart) → 201 + تخزين فعلي على القرص (66 بايت) — **PASSED**
7. تنزيل الدليل يعيد نفس المحتوى الأصلي (200، 66 بايت) — **PASSED**
8. تفاصيل الإجراء تعرض الحالة والأدلة المرفقة — **PASSED**
9. إضافة إجراء يدوي → 201 — **PASSED**
10. RBAC: AUDITOR توليد/إنشاء/تحديث/رفع دليل كلها 403، قراءة 200، تنزيل غير مُصادَق 401 — **PASSED**
11. تسجيل كل العمليات في audit_logs (PROCEDURES_GENERATED/PROCEDURE_CREATE/PROCEDURE_UPDATE/EVIDENCE_UPLOAD) — **PASSED**
12. `npm run build` بلا أخطاء (بعد إصلاح واحد — انظر أدناه) عبر مساراته كافة — **PASSED**
13. عرض شاشتي الإجراءات RTL (القائمة والتفاصيل) بلقطات شاشة فعلية — **PASSED**

## PASSED

13 من 13. تم التحقق من منطق التوليد/التغطية بمعزل عن الواجهة أولًا، ثم كامل التدفق عبر HTTP.

## FAILED (قبل الإصلاح)

- فشل `npm run build` على مسار تنزيل الدليل: تمرير `Buffer` مباشرةً إلى `NextResponse` رفضه TypeScript
  (`Buffer` غير قابل للإسناد إلى `BodyInit`).

## FIXED

- لُفّت البايتات بـ `new Uint8Array(bytes)` قبل تمريرها إلى `NextResponse`؛ أُعيد البناء بنجاح،
  واختُبر التنزيل فعليًا فأعاد المحتوى الأصلي (66 بايت) بحالة 200، و401 دون مصادقة.

## SECURITY

- كل مسارات الكتابة (توليد/إنشاء/تحديث/رفع دليل) تتطلب ADMIN (403 لـ AUDITOR — مُختبَرة على الأربعة) وجلسة (401)
- لا يمكن إنهاء إجراء (DONE) دون استنتاج موثّق — مطبّق في الخدمة والـAPI
- بايتات الأدلة تُخزَّن على القرص؛ البيانات الوصفية (اسم/حجم/مَن/متى) في القاعدة؛ التنزيل يتطلب جلسة (401 دونها)
- كل عملية مُسجَّلة في audit_logs؛ سجل التدقيق يبقى بلا مسار حذف (Invariant #7 محفوظ — أكّده بقاء
  سجل PROCEDURES_GENERATED من اختبار خدمة معزول رغم حذف بقية بياناته)
- **لم يُنجز بعد**: فحص نوع/فيروسات لملفات الأدلة، حذف/إصدارات للأدلة، rate limiting

## REMAINING (لهذه المرحلة تحديدًا)

- لا حذف/تعديل لإصدارات الأدلة بعد (الرفع والتنزيل فقط)
- التغطية تعتمد مطابقة (نوع-مخاطرة + حساب)؛ لا ربط يدوي لإجراء بملاحظات متعددة بعد
- لا تصدير لتقرير الإجراءات/الأدلة (PDF/Excel) بعد
- تنفيذ الإجراءات مقصور على المدير (نموذج الصلاحيات الحالي: ADMIN كتابة/AUDITOR اطلاع) — دور "منفّذ" منفصل مؤجَّل

## NEXT — Phase 5 (حسب ترتيب المواصفة)

- وحدة الملاحظات والتسويات (Findings & Adjustments) المبنية على الإجراءات والأدلة
- ثم مراجعة الإدارة، لإكمال حساب Audit Readiness Score الكامل من المواصفة

---

# PHASE 5 — الملاحظات والتسويات ومراجعة الإدارة + مؤشر الجاهزية الكامل

```
PHASE:    5 — Findings, Adjustments, Management Review & full Audit Readiness Score
STATUS:   COMPLETE and VERIFIED (التسويات قيود محاسبية متوازنة فعليًا، الميزان المعدّل
          والمؤشر محسوبان من حالة قاعدة البيانات الحقيقية. اختُبر منطق DB أولًا ثم كامل
          التدفق عبر HTTP + لقطات شاشة)
```

## IMPLEMENTED

- **الملاحظات (Findings)**: رفع ملاحظة من إجراء تدقيقي أو يدويًا (عنوان/خطورة/توصية/حساب)، **رد الإدارة**،
  وحالة المعالجة (مفتوحة/عولجت/مخاطرة مقبولة)
- **التسويات (Adjustments)**: قيود تسوية بسطور مدين/دائن **يجب أن تتوازن (مدين = دائن)** — يُرفض غير المتوازن،
  ولا يُعتمد إلا متوازنًا (تحقّق مزدوج عند الاقتراح وعند الاعتماد)، مع ربط اختياري بملاحظة
- **الميزان المعدّل (Adjusted TB)**: الميزان المستورد + التسويات **المعتمدة فقط** لكل حساب، محسوب عبر SQL
  (يشمل حسابات جديدة تظهر في التسوية فقط)
- **مراجعة الإدارة**: سجل قرارات (اعتماد/إعادة) بأثر رجعي (append-only) مع المراجِع والوقت
- **مؤشر جاهزية التدقيق الكامل (Audit Readiness Score)** من المواصفة (القسم 8): مرجّح شفاف عبر 8 أبعاد
  محسوبة من الحالة الفعلية — جودة البيانات (20)، التسوية (15)، تقييم المخاطر (10)، تغطية الإجراءات (20)،
  الأدلة (10)، معالجة الملاحظات (15)، بتّ التسويات (5)، مراجعة الإدارة (5) — يظهر على لوحة التحكم بأشرطة تفصيلية
- REST API مطابق: `GET/POST /api/findings`, `GET/PATCH /api/findings/[id]`,
  `GET/POST /api/adjustments`, `GET/PATCH /api/adjustments/[id]`, `GET /api/adjusted-trial-balance`,
  `GET/POST /api/management-review`, `GET /api/readiness`
- تسجيل كل عملية في `audit_logs`: FINDING_CREATE/UPDATE, ADJUSTMENT_PROPOSE/APPROVED/REJECTED, MANAGEMENT_REVIEW

## TESTS (منطق DB مُتحقَّق أولًا + كامل التدفق عبر HTTP + لقطات شاشة)

1. إنشاء ملاحظة (يدوية/من إجراء) → 201، وتحديثها إلى "عولجت" مع رد الإدارة → 200 — **PASSED**
2. توازن التسوية: قيد غير متوازن (1000/900) → 400؛ متوازن (10000/10000) → 201 — **PASSED**
3. اعتماد التسوية → 200، ويُعاد التحقق من التوازن قبل الاعتماد — **PASSED**
4. الميزان المعدّل ينعكس فعليًا: Cash 1010 200,000→210,000، AR 1200 150,000→140,000، والإجمالي يبقى 0 — **PASSED**
5. حساب جديد يظهر في التسوية فقط يظهر في الميزان المعدّل (UNION) — **PASSED** (اختبار DB)
6. مراجعة الإدارة (اعتماد) → 201 وتظهر كآخر قرار — **PASSED**
7. مؤشر الجاهزية الكامل محسوب عبر 8 أبعاد (خط أساس 60 → 65 بعد المراجعة) بتفصيل شفاف لكل بُعد — **PASSED**
8. RBAC: AUDITOR إنشاء ملاحظة/اقتراح تسوية/مراجعة إدارة كلها 403؛ القراءة (ملاحظات/جاهزية) 200؛ غير مُصادَق 401 — **PASSED**
9. تسجيل كل العمليات في audit_logs — **PASSED**
10. `npm run build` بلا أخطاء (بعد إصلاح واحد) عبر 36 مسارًا — **PASSED**
11. عرض لوحة التحكم (المؤشر الكامل + الأبعاد + مراجعة الإدارة) وشاشتي الملاحظات/التسويات RTL — لقطات — **PASSED**

## PASSED

11 من 11. تم التحقق من منطق التسوية/الميزان المعدّل بمعزل عن الواجهة أولًا، ثم كامل التدفق عبر HTTP.

## FAILED (قبل الإصلاح)

- فشل `npm run build`: `raiseFinding` مرّر `...input` (يحوي `userId` بلا `createdBy`) إلى `createFinding`
  التي تتطلب `createdBy` — عدم تطابق أنواع في TypeScript.

## FIXED

- أُعيدت كتابة الاستدعاء لتعيين الحقول صراحةً (`createdBy: input.userId`)؛ نجح البناء واختُبر التدفق كاملًا عبر HTTP.

## SECURITY

- كل مسارات الكتابة (ملاحظة/تسوية/اعتماد/مراجعة) تتطلب ADMIN (403 لـ AUDITOR — مُختبَرة) وجلسة (401)
- **قيد التسوية غير المتوازن يُرفض** عند الاقتراح، ويُعاد التحقق من التوازن قبل الاعتماد (دفاع بعمق)
- الميزان المعدّل يعتمد التسويات **المعتمدة فقط** (المقترحة/المرفوضة لا تؤثر)
- كل عملية مُسجَّلة في audit_logs؛ سجل التدقيق يبقى بلا مسار حذف (Invariant #7 محفوظ)
- **لم يُنجز بعد**: توقيع إلكتروني/قفل لمراجعة الإدارة، تصدير القوائم المالية المعدّلة، rate limiting

## REMAINING (لهذه المرحلة تحديدًا)

- نموذج اقتراح التسوية يعرض 4 سطور ثابتة (يكفي لمعظم القيود؛ سطور ديناميكية لاحقًا)
- لا ترحيل فعلي للتسويات إلى الأستاذ العام (تبقى طبقة تسوية منفصلة تظهر في الميزان المعدّل — وهو السلوك الصحيح لمرحلة ما قبل التدقيق)
- مؤشر الجاهزية على مستوى الشركة (لا تصفية لكل سنة مالية بعد)
- لا تصدير لتقرير الجاهزية/الملاحظات (PDF)

## NEXT — Phase 6 (حسب ترتيب المواصفة)

- تصدير حزمة ما قبل التدقيق (Audit Pack): الميزان المعدّل + الملاحظات + الإجراءات والأدلة + مؤشر الجاهزية كـ PDF/Excel
- لوحات متابعة عبر الشركات/السنوات المالية وصلاحيات أدق (دور "منفّذ")

---

# PHASE 6 — تصدير حزمة ما قبل التدقيق (Audit Pack)

```
PHASE:    6 — Audit Pack Export (Excel + print-ready report)
STATUS:   COMPLETE and VERIFIED (ملف Excel حقيقي بـ9 أوراق مبني بالكامل من قاعدة
          البيانات، تم تنزيله وفتحه والتحقق من أوراقه وقيمه؛ وصفحة تقرير عربية RTL
          جاهزة للطباعة/PDF)
```

## IMPLEMENTED

- **مُولّد حزمة تدقيق Excel** (`src/lib/audit-pack.ts`) عبر exceljs — مصنّف بـ**9 أوراق RTL** مبنية
  كلها من البيانات الفعلية: الملخص + مؤشر الجاهزية، ميزان المراجعة، الميزان المعدّل، الأستاذ العام،
  سجل المخاطر، الإجراءات، الملاحظات، التسويات (بسطورها)، وسجل التدقيق
- **صفحة تقرير جاهزة للطباعة** (`/reports`) تجمّع كل شيء بعربية RTL: بيانات الشركة، مؤشر الجاهزية
  بأبعاده، ملخص المخاطر، الإجراءات، الملاحظات، التسويات، ومراجعة الإدارة — مع **زر طباعة/حفظ PDF**
  (طباعة المتصفح تعطي PDF عربيًا سليمًا، بلا مشاكل تشكيل الحروف)
- REST API: `GET /api/audit-pack?companyId=` يبثّ ملف xlsx باسم ملف يحمل اسم الشركة
- تسجيل كل تصدير في `audit_logs` (AUDIT_PACK_EXPORT)
- التقرير والتصدير يعكسان حالة قاعدة البيانات لحظة الإصدار (لا لقطات مخزّنة)

## TESTS (تنزيل فعلي + قراءة الملف للتحقق + لقطة شاشة)

1. تصدير xlsx → 200 وملف فعلي (~18KB) — **PASSED**
2. قراءة الملف المُصدَّر: 9 أوراق بأسماء عربية صحيحة موجودة — **PASSED**
3. ورقة الملخص تحوي مؤشر الجاهزية الفعلي (`45/100 (LOW)` وقت التصدير) — **PASSED**
4. ورقة الميزان المعدّل تحوي الحسابات والقيم الصحيحة (تسوية = 0 قبل الاعتماد — سلوك صحيح) — **PASSED**
5. تصدير غير مُصادَق → 401 — **PASSED**
6. صفحة `/reports` تعرض التقرير الكامل RTL (جاهزية + مخاطر + إجراءات + ملاحظات + تسوية معتمدة متوازنة + مراجعة) بلقطة شاشة — **PASSED**
7. تسجيل AUDIT_PACK_EXPORT في audit_logs — **PASSED**
8. `npm run build` بلا أخطاء عبر 38 مسارًا — **PASSED**

## PASSED

8 من 8. تم تنزيل الملف المُصدَّر وقراءته برمجيًا للتحقق من الأوراق والقيم (لا افتراض).

## FAILED (قبل الإصلاح)

- لا شيء في هذه المرحلة — الاعتماد على exceljs (المُختبَرة في Phase 2) وقراءة الملف الناتج للتحقق
  منعا المفاجآت. (قرار تصميمي واعٍ: تجنّب PDF عربي مُشكَّل يدويًا لأنه ينتج نصًا مكسورًا =
  Placeholder فعلي؛ استُخدمت طباعة المتصفح لعربية سليمة.)

## FIXED

- لا إصلاحات لازمة في هذه المرحلة.

## SECURITY

- التصدير والتقرير للقراءة فقط ويتطلبان جلسة (401 دونها)؛ متاحان للمراجع (اطلاع)
- كل تصدير مُسجَّل في audit_logs (AUDIT_PACK_EXPORT)
- الملف يُبنى في الذاكرة ويُبثّ مباشرةً (لا يُكتب على القرص)؛ اسم الملف مُعقَّم
- سجل التدقيق يبقى بلا مسار حذف (Invariant #7 محفوظ)
- **لم يُنجز بعد**: جدولة/أرشفة نسخ الحزمة، توقيع رقمي للتقرير، rate limiting

## REMAINING (لهذه المرحلة تحديدًا)

- تصدير PDF مباشر من الخادم غير مُنجَز (يُعتمد على طباعة المتصفح لضمان عربية سليمة)
- لا لوحة متابعة عبر عدة شركات/سنوات مالية بعد (portfolio view)
- التقرير يعرض أول 30 ملاحظة مخاطر (الكامل في ملف Excel) — مذكور صراحةً في الصفحة

## NEXT (تحسينات مستقبلية اختيارية)

- لوحة portfolio عبر الشركات، دور "منفّذ" منفصل، جدولة تصدير دورية، وتوقيع رقمي للتقارير

---

# PHASE 7 — لوحة المحفظة (Portfolio) عبر الشركات

```
PHASE:    7 — Firm-level Portfolio Overview
STATUS:   COMPLETE and VERIFIED (تجميع لكل الشركات من المحركات الحقيقية نفسها؛ اختُبر
          بعدة شركات عبر HTTP + لقطة شاشة. كشف الاختبار خطأ نمذجة في المؤشر وخطأ نشر
          في التغليف — أُصلحا وأُعيد التحقق.)
```

## IMPLEMENTED

- **محرك المحفظة** (`src/lib/portfolio.ts`): لكل شركة يعيد استخدام محركات المراحل السابقة نفسها
  (الجاهزية، المخاطر، الملاحظات، الإجراءات، التغطية، آخر مراجعة/استيراد) — لا منطق مكرَّر
- **إجماليات المحفظة**: عدد الشركات، الشركات ذات البيانات، متوسط الجاهزية، الجاهزة، الملاحظات المفتوحة،
  المخاطر المرتفعة
- **ترتيب بالأولوية**: الشركات ذات البيانات أولًا مرتّبة تصاعديًا بالجاهزية (الأقل جاهزية = الأحوج للانتباه)،
  ثم الشركات بلا بيانات
- **شاشة `/portfolio`** ببطاقات إجمالية وجدول لكل شركة مع روابط للوحة كل شركة
- REST API: `GET /api/portfolio`

## TESTS (عدة شركات ببيانات متفاوتة عبر HTTP + لقطة شاشة)

1. 3 شركات (كاملة/ميزان فقط/بلا بيانات) → الإجماليات صحيحة (companies=3, withData=2) — **PASSED**
2. الترتيب: الأقل جاهزية أولًا، وبلا بيانات أخيرًا — **PASSED**
3. RBAC: AUDITOR قراءة 200، غير مُصادَق 401 — **PASSED**
4. عرض الشاشة RTL بالبطاقات والجدول — لقطة شاشة — **PASSED**
5. `npm run build` عبر 40 مسارًا + CSS كامل (~17KB) في بناء نظيف من الحزمة المُسلَّمة — **PASSED**

## PASSED

5 من 5 (بعد إصلاحين كشفهما الاختبار).

## FAILED (قبل الإصلاح)

- **خطأ نمذجة في مؤشر الجاهزية**: شركة لديها ميزان فقط (بلا أستاذ عام ولا أهمية نسبية) سجّلت جاهزية
  73 — أعلى من شركة ببيانات كاملة (65)! السبب: بُعدا "التسوية" و"التغطية" كانا يمنحان درجة كاملة عند
  غياب المخاطر المكتشفة، فيُكافأ نقص البيانات بدل معاقبته.
- **خطأ نشر في التغليف**: أمر تنظيف استخدم `rm *.mjs` فحذف `next.config.mjs` و`postcss.config.mjs`
  من حزمتَي Phase 5 و6 المُصدَّرتين — وبدون postcss لا يعالج Tailwind توجيهاته فينتج CSS شبه فارغ
  (تنسيق مكسور عند بناء المستخدم). (البناء ينجح شكليًا لأن Next وTailwind لا يفشلان بصوت عالٍ.)

## FIXED

- المؤشر: بُعد "التسوية" لا يمنح جزء الميزان/الأستاذ (7) إلا بوجود أستاذ عام؛ بُعد "التغطية" يُعتبر
  "غير مُقيَّم" (0) ما لم يوجد أستاذ عام + أهمية نسبية. أُعيد التحقق: بيتا هبطت إلى 46 (< ألفا 65)
  والترتيب صار صحيحًا.
- التغليف: أُعيد إنشاء `next.config.mjs` و`postcss.config.mjs`، واستُبدل التنظيف بحذف صريح بلا `*.mjs`،
  وتأكّد أن بناءً نظيفًا من الحزمة ينتج CSS كاملًا (17,274 بايت مع أصناف Tailwind).
- **ملاحظة للمستخدم**: حزمتا Phase 5/6 السابقتان تنقصهما هذان الملفان — حزمة Phase 7 (النظام الكامل)
  مُصحَّحة، أو يكفي إضافة الملفين لأي حزمة سابقة.

## SECURITY

- المحفظة للقراءة فقط وتتطلب جلسة (401 دونها)؛ متاحة للمراجع
- لا كتابة في هذه المرحلة؛ لا تغيير على النماذج أو الصلاحيات
- سجل التدقيق يبقى بلا مسار حذف (Invariant #7 محفوظ)

## REMAINING (لهذه المرحلة تحديدًا)

- المحفظة تحسب لكل شركة عند الطلب (كافٍ لعشرات الشركات؛ للأعداد الكبيرة يلزم تخزين مؤقت/تجميع مُسبق)
- لا تصفية/بحث في جدول المحفظة بعد، ولا تصدير له

## NEXT (تحسينات مستقبلية اختيارية)

- تخزين مؤقت لنتائج المحفظة، جدولة تصدير دورية، توقيع رقمي للتقارير

---

# PHASE 8 — دور "منفّذ" وفصل المهام (Separation of Duties)

```
PHASE:    8 — Executor Role & Separation of Duties
STATUS:   COMPLETE and VERIFIED (دور ثالث + إعادة توزيع صلاحيات كل مسارات الكتابة؛
          اختُبرت مصفوفة الأدوار الثلاثة عبر HTTP، وترحيل قاعدة قديمة بأمان + لقطة شاشة)
```

## IMPLEMENTED

- **دور ثالث EXECUTOR ("منفّذ")** بين ADMIN وAUDITOR — يطبّق **فصل المهام** كضابط تدقيق:
  - **المنفّذ يستطيع**: رفع/ربط/التزام الاستيراد، توليد/تحديث الإجراءات، رفع الأدلة، رفع/تحديث الملاحظات،
    **اقتراح** التسويات، وكل القراءة/التصدير
  - **المنفّذ لا يستطيع**: إدارة الشركات/السنوات المالية/المستخدمين، تحديد الأهمية النسبية،
    **اعتماد** التسويات، مراجعة الإدارة — كلها ADMIN فقط
  - **المراجع (AUDITOR)**: اطلاع فقط (كما كان)
- منطق الصلاحيات في مكان واحد: `canExecute(role)` و`requireExecutor()` في `auth.ts`؛ مسارات التنفيذ
  تستخدمها، ومسارات الحوكمة/الاعتماد تبقى `requireAdmin`
- الفصل الجوهري: المنفّذ **يقترح** قيد التسوية والمدير **يعتمده** (لا يعتمد أحدٌ قيدًا اقترحه بنفسه إلزاميًا؛
  الأدوار تفصل الصلاحية)
- ترحيل آمن لقاعدة البيانات: قيد CHECK على `users.role` وُسّع ليشمل EXECUTOR؛ للقواعد القديمة يُعاد بناء
  جدول users بنمط SQLite الآمن (البيانات والمفاتيح الأجنبية محفوظة)
- واجهة: إنشاء مستخدم بدور "منفّذ"، وإخفاء أزرار الاعتماد/الحوكمة عن المنفّذ في الشاشات

## TESTS (مصفوفة الأدوار الثلاثة عبر HTTP + اختبار الترحيل + لقطة شاشة)

1. الترحيل: قاعدة بالقيد القديم (ADMIN/AUDITOR فقط) → بعد فتحها عبر db.ts يقبل EXECUTOR،
   والمستخدم القديم وسجل التدقيق محفوظان، والمفتاح الأجنبي ما زال مُفعَّلًا — **PASSED**
2. المنفّذ: رفع ملاحظة 201، توليد إجراءات 201، اقتراح تسوية (id) — **PASSED**
3. المنفّذ ممنوع: اعتماد تسوية 403، إنشاء شركة 403، تحديد الأهمية 403، إنشاء مستخدم 403، مراجعة الإدارة 403 — **PASSED**
4. المدير يعتمد التسوية التي اقترحها المنفّذ → 200 (فصل مهام) — **PASSED**
5. المراجع: قراءة 200؛ رفع استيراد/ملاحظة/اقتراح تسوية كلها 403 — **PASSED**
6. الترحيل لا يفقد المفاتيح الأجنبية (إدراج بمرجع غير صالح يُرفض) — **PASSED**
7. `npm run build` بلا أخطاء عبر 40 مسارًا + CSS كامل في بناء نظيف — **PASSED**
8. عرض شاشة المنفّذ RTL: نموذج اقتراح ظاهر، بلا أزرار اعتماد، بلا رابط المستخدمين — لقطة — **PASSED**

## PASSED

8 من 8.

## FAILED (قبل الإصلاح)

- لا أخطاء منطقية جديدة في هذه المرحلة. (ملاحظة إجرائية: طُبِّق درس Phase 7 — لا حذف بـ globs،
  والتحقق من CSS في بناء نظيف؛ الترحيل الخطِر لجدول users اختُبر بمعزل قبل الاعتماد عليه.)

## FIXED

- لا إصلاحات لازمة في هذه المرحلة.

## SECURITY

- إعادة توزيع الصلاحيات مركزية عبر `canExecute`/`requireExecutor` — لا فحوص متناثرة متضاربة
- فصل المهام مطبَّق فعليًا: الاعتماد (التسويات، مراجعة الإدارة) والحوكمة (شركات/سنوات/مستخدمون/أهمية)
  محصورة في ADMIN، وتنفيذ التدقيق متاح للمنفّذ — مُختبَر على كل مسار
- الترحيل يعيد تفعيل `PRAGMA foreign_keys=ON` بعد إعادة البناء
- سجل التدقيق يبقى بلا مسار حذف (Invariant #7 محفوظ)
- **لم يُنجز بعد**: تعطيل/تعديل دور مستخدم قائم من الواجهة، سجل تغييرات الأدوار المنفصل

## REMAINING (لهذه المرحلة تحديدًا)

- لا واجهة لتغيير دور مستخدم موجود أو تعطيله (الإضافة فقط — كما وُثّق منذ Phase 1)
- شاشات الحوكمة (الشركة/السنوات المالية) يراها المنفّذ للقراءة؛ نماذجها محصورة في المدير

## NEXT (تحسينات مستقبلية اختيارية)

- تخزين مؤقت للمحفظة، جدولة تصدير دورية، توقيع رقمي للتقارير

---

# PHASE 9 — إدارة المستخدمين الكاملة (تعديل الدور + تعطيل/تفعيل)

```
PHASE:    9 — Full User Administration (role change + enable/disable)
STATUS:   COMPLETE and VERIFIED (يسدّ نقصًا موثّقًا منذ Phase 1؛ اختُبر عبر HTTP +
          ترحيل عمود active على قاعدة سابقة + لقطة شاشة)
```

## IMPLEMENTED

- **تعديل دور مستخدم قائم** (مدير/منفّذ/مراجع) و**تعطيله/تفعيله** من صفحة المستخدمين (مدير فقط)
- **عمود `users.active`** (1 نشط / 0 معطَّل) مع ترحيل آمن للقواعد السابقة (`ALTER TABLE ADD COLUMN`)
- **منع دخول المستخدم المعطَّل**: `attemptLogin` يرفض الحساب المعطَّل (يظهر "الحساب معطَّل" في شاشة الدخول)
- **ضابط أمان جوهري (منع القفل)**: لا يمكن **تخفيض** أو **تعطيل** آخر مدير نشط (`countActiveAdmins() <= 1` → 409)
- REST API: `PATCH /api/users/[id]` (الدور و/أو الحالة) — مدير فقط، عبر منطق واحد في `user-actions.ts`
- تسجيل كل تغيير في `audit_logs`: USER_ROLE_CHANGE, USER_ENABLE, USER_DISABLE

## TESTS (HTTP + اختبار الترحيل + لقطة شاشة)

1. ترحيل: قاعدة بلا عمود active → بعد فتحها يُضاف العمود والمستخدمون القدامى نشطون (active=1) — **PASSED**
2. ترقية مستخدم مراجع → منفّذ (PATCH role) → 200 والدور تغيّر فعليًا — **PASSED**
3. تعطيل مستخدم → محاولة دخوله ترجع 401 (معطَّل) — **PASSED**
4. إعادة تفعيله → دخوله ينجح 200 — **PASSED**
5. منع تخفيض آخر مدير نشط → 409 — **PASSED**
6. منع تعطيل آخر مدير نشط → 409 — **PASSED**
7. RBAC: غير المدير (منفّذ) PATCH مستخدم → 403 — **PASSED**
8. تسجيل USER_ROLE_CHANGE / USER_DISABLE / USER_ENABLE في audit_logs — **PASSED**
9. `npm run build` بلا أخطاء + CSS كامل في بناء نظيف — **PASSED**
10. عرض صفحة المستخدمين RTL بقوائم الدور/الحالة وزر الحفظ — لقطة — **PASSED**

## PASSED

10 من 10.

## FAILED (قبل الإصلاح)

- لا أخطاء في الكود. (خطأ في سكربت الاختبار فقط: `UID` متغيّر محجوز في bash أدّى لاستدعاء
  /api/users/0 → 404؛ صُحِّح باسم متغيّر آخر وأُعيد الاختبار بنجاح — لا علاقة له بكود التطبيق.)

## FIXED

- لا إصلاحات في الكود لازمة.

## SECURITY

- تعديل المستخدمين مدير فقط (403 لغيره — مُختبَر)
- منع القفل: آخر مدير نشط محميّ من التخفيض والتعطيل (409)
- الحساب المعطَّل لا يستطيع الدخول (يُرفض في المصادقة)
- كل تغيير مُسجَّل في audit_logs؛ سجل التدقيق يبقى بلا مسار حذف (Invariant #7 محفوظ)
- الترحيل عبر ALTER ADD COLUMN (آمن، بلا إعادة بناء)
- **لم يُنجز بعد**: إعادة تعيين كلمة مرور مستخدم من الواجهة، سجل جلسات نشطة/إنهاء جلسة عن بُعد

## REMAINING (لهذه المرحلة تحديدًا)

- لا إعادة تعيين كلمة مرور من الواجهة (تبقى عبر seed للحساب الأول)
- لا حذف مستخدم (التعطيل هو البديل الصحيح للحفاظ على سلامة سجل التدقيق والمراجع)

## NEXT (تحسينات مستقبلية اختيارية)

- تخزين مؤقت للمحفظة، جدولة تصدير دورية، توقيع رقمي للتقارير

---

# PHASE 10 — إدارة كلمات المرور

```
PHASE:    10 — Password Management (self-change + admin reset)
STATUS:   COMPLETE and VERIFIED (يسدّ نقصًا موثّقًا منذ Phase 1؛ اختُبر عبر HTTP
          بما في ذلك التأكد من عدم تسرّب كلمة المرور في سجل التدقيق + لقطة شاشة)
```

## IMPLEMENTED

- **تغيير المستخدم كلمة مروره بنفسه** (صفحة "حسابي"): يتطلب كلمة المرور الحالية + الجديدة (٨ أحرف فأكثر)،
  ويرفض تطابق الجديدة مع الحالية
- **إعادة تعيين المدير كلمة مرور مستخدم آخر** من صفحة المستخدمين (مدير فقط)
- كلمات المرور تُجزّأ دائمًا بـ bcrypt؛ **لا تُكتب كلمة المرور نصًّا في أي مكان** (ولا في سجل التدقيق)
- REST API: `POST /api/account/password` (الذات) و`POST /api/users/[id]/password` (المدير)
- تسجيل في `audit_logs`: PASSWORD_CHANGE (الذات)، PASSWORD_RESET (المدير) — دون قيمة كلمة المرور

## TESTS (HTTP + لقطة شاشة)

1. تغيير ذاتي بكلمة حالية خاطئة → 400 — **PASSED**
2. تغيير ذاتي لكلمة مطابقة للحالية → 400 — **PASSED**
3. تغيير ذاتي صحيح → 200؛ الدخول بالقديمة يفشل 401 والجديدة ينجح 200 — **PASSED**
4. إعادة تعيين المدير → 200؛ المستخدم يدخل بالكلمة الجديدة 200 — **PASSED**
5. إعادة تعيين بكلمة قصيرة → 400 — **PASSED**
6. RBAC: غير المدير يعيد تعيين كلمة مرور آخر → 403 — **PASSED**
7. سجل التدقيق يحوي PASSWORD_CHANGE/PASSWORD_RESET، **وبلا أي تسرّب لكلمة المرور في التفاصيل (0)** — **PASSED**
8. `npm run build` بلا أخطاء + CSS كامل في بناء نظيف — **PASSED**
9. عرض صفحة "حسابي" RTL — لقطة — **PASSED**

## PASSED

9 من 9.

## FAILED (قبل الإصلاح)

- لا أخطاء في هذه المرحلة.

## FIXED

- لا إصلاحات لازمة.

## SECURITY

- التغيير الذاتي يتحقق من كلمة المرور الحالية قبل القبول
- إعادة التعيين مدير فقط (403 لغيره — مُختبَر)
- كلمة المرور لا تُسجَّل نصًّا في audit_logs (مُتحقَّق برمجيًا: 0 تسرّب) — تُسجَّل الواقعة فقط
- bcrypt (10 rounds) كما في Phase 1؛ سجل التدقيق يبقى بلا مسار حذف (Invariant #7 محفوظ)
- **لم يُنجز بعد**: سياسة تعقيد كلمة مرور متقدمة، إلزام تغيير عند أول دخول، إنهاء الجلسات القائمة عند تغيير الكلمة

## REMAINING (لهذه المرحلة تحديدًا)

- الجلسة الحالية (JWT 8 ساعات) تبقى صالحة بعد تغيير الكلمة حتى انتهائها (لا قائمة إبطال جلسات بعد)
- لا "نسيت كلمة المرور" ذاتي عبر البريد (إعادة التعيين عبر المدير — مناسب لأداة داخلية)

## NEXT (تحسينات مستقبلية اختيارية)

- تخزين مؤقت للمحفظة، جدولة تصدير دورية، توقيع رقمي للتقارير

---

# PHASE 11 — إبطال الجلسات الفوري (Session Invalidation)

```
PHASE:    11 — Immediate Session Invalidation
STATUS:   COMPLETE and VERIFIED (سدّ ثغرة أمنية حقيقية: الجلسات كانت تبقى صالحة 8 ساعات
          بعد تغيير الدور/التعطيل/كلمة المرور. اختُبر عبر HTTP + تسلسل الترحيلات)
```

## المشكلة التي عُولجت

كانت الجلسة (JWT صالح 8 ساعات) تحمل الدور وقت الإصدار ولا تتحقق من قاعدة البيانات، فينتج عن ذلك:
مديرٌ مُخفَّض يحتفظ بصلاحياته حتى انتهاء الرمز، ومستخدمٌ مُعطَّل تبقى جلسته تعمل، وتغيير كلمة المرور
لا يُبطل الجلسات الأخرى.

## IMPLEMENTED

- **`getSession` يتحقق حيًّا من قاعدة البيانات** في كل طلب: يرفض الجلسة إذا حُذف المستخدم أو **عُطِّل**
  (active=0) أو أصبح إصدار الرمز `token_epoch` قديمًا؛ ويعيد **الدور والاسم والبريد محدثة من القاعدة**
- **تغيير الدور يسري فورًا** دون إعادة تسجيل دخول (الدور يُقرأ حيًّا) — المدير المُخفَّض يفقد صلاحياته في الطلب التالي
- **التعطيل يسري فورًا** (فحص active في كل طلب)
- **عمود `token_epoch`** يُرفع عند تغيير كلمة المرور → تُبطَل كل الجلسات الأخرى؛ وجلسة من غيّر كلمته
  تُعاد إصدارها بالكوكي المحدَّث فيبقى داخلًا
- ترحيل آمن (`ALTER ADD COLUMN`)، ويتسلسل بأمان مع ترحيلَي Phase 8/9 على القواعد القديمة

## TESTS (HTTP + تسلسل الترحيلات)

1. مستخدم منفّذ يرفع ملاحظة 201؛ بعد تخفيضه لمراجع **بنفس الكوكي** → الكتابة 403 والقراءة 200 (الدور تغيّر فورًا) — **PASSED**
2. تعطيل المستخدم → جلسته القائمة بنفس الكوكي ترجع 401 فورًا — **PASSED**
3. تغيير كلمة المرور من جهاز → جهاز آخر (جلسة أخرى) يُبطَل 401، وجلسة نفس الجهاز تبقى 200 بالكوكي المحدَّث — **PASSED**
4. تأكيد إصدار Set-Cookie عند تغيير كلمة المرور الذاتي — **PASSED**
5. تسلسل الترحيلات الثلاثة (EXECUTOR rebuild + active + token_epoch) على قاعدة ما قبل Phase 8: كل الأعمدة مضافة،
   والبيانات والمفاتيح الأجنبية محفوظة — **PASSED**
6. `npm run build` بلا أخطاء + CSS كامل في بناء نظيف — **PASSED**

## PASSED

6 من 6. (الحالة الوحيدة التي بدت فاشلة كانت أثر اختبار: عدم حفظ الكوكي المحدَّث بـ curl `-c`؛ في المتصفح
يتحدّث تلقائيًا — أُعيد الاختبار بحفظ الكوكي فبقيت الجلسة صالحة.)

## FAILED (قبل الإصلاح)

- لا أخطاء في الكود. تبسيط تصميمي أثناء البناء: أُلغي رفع الإصدار عند تغيير الدور/التعطيل (لا لزوم له
  لأن getSession يقرأ الدور والحالة حيًّا)، وأُبقي الرفع لكلمة المرور فقط (إبطال الجلسات الأخرى).

## FIXED

- لا إصلاحات لازمة.

## SECURITY

- **إبطال فوري**: التعطيل وتغيير الدور وتغيير كلمة المرور تسري كلها في الطلب التالي — لا نافذة 8 ساعات بعد الآن
- الرمز يحمل معرّف المستخدم + الإصدار فقط؛ الدور يُقرأ من القاعدة (لا يمكن تزوير الدور في الرمز)
- كلفة: استعلام مستخدم واحد لكل طلب (SQLite داخل العملية — سريع)
- سجل التدقيق يبقى بلا مسار حذف (Invariant #7 محفوظ)
- **لم يُنجز بعد**: "تسجيل خروج من كل الأجهزة" كزر صريح (متاح ضمنيًا بتغيير كلمة المرور)، انتهاء خمول (idle timeout)

## REMAINING (لهذه المرحلة تحديدًا)

- لا زر صريح "إنهاء كل الجلسات" (يتحقق فعليًا عبر تغيير كلمة المرور الذي يرفع الإصدار)
- لا مهلة خمول منفصلة عن انتهاء الرمز (8 ساعات)

---

# PHASE 12 — تحسينات تشغيلية وسلامة (Ops & Integrity)

```
PHASE:    12 — Terminate-all-sessions + Portfolio cache + Report integrity signature
STATUS:   COMPLETE and VERIFIED (ثلاث تحسينات حقيقية اختُبرت عبر HTTP؛ كُشف خطأ ترتيب
          في التوقيع وأُصلح)
```

## IMPLEMENTED

1. **إنهاء كل الجلسات** (زر في "حسابي"): يرفع `token_epoch` فيُبطل جلسات المستخدم على بقية الأجهزة،
   ويعيد إصدار الجلسة الحالية فتبقى نشطة. API: `POST /api/account/logout-all`. تسجيل SESSIONS_TERMINATED.
2. **تخزين مؤقت للمحفظة** — تخزين داخل العملية مفتاحه **عدّاد الكتابات العام** (يُرفع في `writeAuditLog`
   مع كل عملية حسّاسة)؛ يُعاد الحساب **فقط بعد كتابة فعلية** → لا تقادم إطلاقًا. تسريع القراءات المتكررة.
3. **توقيع/سلامة التقرير** — بصمة **SHA-256** لمحتوى التقرير القانوني (الجاهزية/المخاطر/الميزان المعدّل/
   الملاحظات/التسويات/المراجعة، باستثناء طابع الوقت). التوقيع يسجّل البصمة+المُوقِّع+الوقت؛ وعند العرض تُعاد
   البصمة وتُقارن → **كشف أي تغيّر في البيانات بعد التوقيع** (drift/tamper). API: `GET/POST /api/reports/[companyId]/sign`.
   جدول `report_signatures`. تسجيل REPORT_SIGN. لوحة سلامة على صفحة التقرير مع زر إعادة التوقيع (مدير فقط).

- ملاحظة عن "جدولة التصدير الدورية": شأن بنية تحتية خارج التطبيق (Next بلا مشغّل مهام دائم)؛ مسار التصدير
  `GET /api/audit-pack` جاهز ليُستدعى من مجدول خارجي — لم يُزيَّف مجدول داخلي.

## TESTS (HTTP + لقطة شاشة)

1. المحفظة: عدد الشركات 1 ثم إضافة شركة ثم 2 — الكاش أُبطِل تلقائيًا بعد الكتابة — **PASSED**
2. إنهاء الجلسات: جهاز2 صالح قبل؛ جهاز1 ينهي الكل → جهاز1 يبقى 200 وجهاز2 يُبطَل 401 — **PASSED**
3. توقيع: توقيع → مطابق True؛ تغيير المحتوى (رفع ملاحظة) → مطابق False (كشف التغيّر)؛ إعادة توقيع → مطابق True (بأحدث ملاحظة) — **PASSED**
4. RBAC: غير المدير يوقّع التقرير → 403 — **PASSED**
5. `npm run build` بلا أخطاء (43 مسارًا) + CSS كامل في بناء نظيف — **PASSED**
6. عرض لوحة سلامة التقرير RTL — لقطة — **PASSED**

## PASSED

6 من 6 (بعد إصلاح واحد كشفه الاختبار).

## FAILED (قبل الإصلاح)

- **إعادة التوقيع لم تُطابِق**: توقيعان في نفس الثانية لهما `signed_at` متطابق، فـ`ORDER BY signed_at DESC`
  كان قد يعيد التوقيع الأقدم (تعادل غير محسوم في SQLite).

## FIXED

- أُضيف فاصل ترتيب `rowid DESC` إلى `latestReportSignature` فيُضمن دائمًا أحدث توقيع بالإدراج؛ أُعيد
  الاختبار على خادم نظيف فطابقت إعادة التوقيع. (البصمة نفسها مُتحقَّق من حتميّتها: 3 حسابات متتالية متطابقة.)

## SECURITY

- إنهاء الجلسات يستخدم آلية الإبطال نفسها من Phase 11
- الكاش لا يُدخل تقادمًا (مفتاحه عدّاد الكتابات) — لا يعرض بيانات قديمة
- توقيع التقرير مدير فقط (403 لغيره)؛ البصمة تكشف أي عبث بالبيانات بعد الاعتماد
- سجل التدقيق يبقى بلا مسار حذف (Invariant #7 محفوظ)

## REMAINING

- لا جدولة تصدير داخل التطبيق (تُدار خارجيًا عبر مسار التصدير)
- توقيع التقرير سلامة محتوى (hash) لا توقيع تشفيري بمفتاح خاص (PKI) — كافٍ لكشف التغيّر داخليًا

## NEXT (اختياري متقدّم)

- توقيع PKI بمفتاح خاص، تصدير مجدول عبر بنية خارجية، تنبيهات بريدية

---

## PHASE 2+ — تحسين استيراد أودو (Odoo General Ledger)

STATUS:   COMPLETE and VERIFIED (اختُبر على ملف أودو حقيقي: 126,873 قيدًا)

### IMPLEMENTED
- محوّل جديد `src/lib/odoo.ts` يتعرّف تلقائيًا على تقرير «دفتر الأستاذ العام» المُصدَّر من أودو
  (تنسيق مُجمّع: عنوان + ترويسة في صف غير أول + رمز الحساب على صف رأس الحساب فقط + صفوف
  مجاميع/أرصدة افتتاحية بين القيود) ويُسطّحه إلى صف واحد لكل قيد.
- يملأ رمز الحساب واسمه تنازليًا (forward-fill) على كل سطر قيد، ويُسقط صفوف المجموعات
  والمجاميع والرصيد الافتتاحي، ويحفظ رقم الصف الأصلي (source_row) لكل قيد لتتبّع المصدر.
- مربوط داخل `parseFile` (excel.ts) قبل القارئ العام؛ يعمل للـ xlsx والـ csv.
- أضيفت مرادفات أعمدة أودو: «رمز» (رمز الحساب) و«التواصل» (البيان).

### TESTS / PASSED
- ملف أودو الحقيقي (يناير 2026): تعرّف = GENERAL_LEDGER (ثقة 1.0)، ربط تلقائي كامل للأعمدة الثمانية.
- الاستيراد: 126,873 قيدًا، الحالة COMMITTED، التسوية متوازنة (مدين=دائن=58,413,957.71، فرق 0).
- سلامة الملفات المسطّحة القائمة: عيّنات TB/GL لم تتأثر (المحوّل يُعيد null لغير تقارير أودو).
- البناء الكامل `npm run build`: ✓ Compiled successfully، فحص الأنواع سليم.

### SECURITY
- المحوّل قراءة/تحويل فقط؛ لا يغيّر مسارات الصلاحيات ولا التسوية الإلزامية ولا سجل التدقيق.

---

## PHASE 3+ — أداء التحليل على البيانات الكبيرة (Large-ledger performance)

STATUS:   COMPLETE and VERIFIED (اختُبر على 126,873 قيدًا)

### المشكلة (FAILED قبل الإصلاح)
- بعد استيراد أستاذ عام كبير (126,873 قيدًا)، كان `analyzeRisk` يولّد **60,433 ملاحظة**
  وحمولة JSON بحجم **12.8 ميجابايت**، ويستغرق ~6 ثوانٍ، و`computeFullReadiness` ~12 ثانية.
  العمل متزامن على خيط الخادم الوحيد، فكان يُجمّد كل صفحة أثناء الحساب، والمتصفح يتجمّد
  بمحاولة عرض 60 ألف صف. النتيجة: «المنصة تعلّق عند اختيار أي قسم بعد رفع الملف».

### FIXED
- **حدّ عرض الملاحظات:** 100 لكل نوع كحدّ أقصى في قائمة العرض (`flags`)، مع الاحتفاظ
  بالأعداد الإجمالية الصحيحة في ملخّص الإجراءات. (60,433 → ~300-400 ملاحظة، 12.8MB → ~80KB.)
- **مفاتيح مخاطر كاملة منفصلة (`riskKeys`):** توليد الإجراءات وتغطيتها يعتمد على مجموعة
  المفاتيح المميّزة الكاملة (غير محدودة) لا على قائمة العرض المحدودة — فلا نقص في التغطية.
- **تخزين مؤقت (memoization):** نتيجة `analyzeRisk` تُخزَّن بمفتاح (الشركة + الأهمية +
  عدّاد الكتابات)، فتُحسب مرّة واحدة لكل تغيّر بيانات وتُعاد فورًا بعدها (0ms).

### TESTS / PASSED (على الحزمة بعد بناء نظيف)
- الاستيراد: 126,873 قيدًا COMMITTED متوازن.
- `analyzeRisk`: أول مرة ~4 ثوانٍ (لمرّة واحدة)، ثم **0ms** (مخزّن)؛ العرض 300-400 ملاحظة، JSON ~80KB.
- `computeFullReadiness`: من ~12 ثانية إلى ~0-2 ثانية؛ الدرجة والأعداد بلا تغيّر.
- الإجراءات/التغطية: 151 اقتراحًا (مبنية على المفاتيح الكاملة، لا نقص).
- درجة المخاطر والأعداد الإجمالية (نهاية الأسبوع 39,519، مكرّرة 19,590 مجموعة …) صحيحة كما هي.

---

## PHASE 2++ — رفع عدة ملفات دفعة واحدة + حدّ حجم الرفع

STATUS:   COMPLETE and VERIFIED

### FIXED (حرِج) — حدّ حجم رفع الخادم
- كان `next.config.mjs` بلا `serverActions.bodySizeLimit`، فالحدّ الافتراضي 1MB يمنع رفع
  ملفات أودو الحقيقية (شهر واحد ~6MB). رُفع الحدّ إلى 150MB ليغطّي الرفع المتعدد.

### IMPLEMENTED — الاستيراد المتعدد
- `autoImportOne` في import-actions.ts: رفع + اكتشاف تلقائي + ربط تلقائي + تحقّق + تسوية +
  اعتماد لملف واحد. الملفات غير المتوازنة (فرق تسوية) تُحظر ولا تُعتمد؛ الملفات التي يتعذّر
  ربطها تلقائيًا تبقى بانتظار ربط يدوي.
- نموذج «رفع عدة ملفات دفعة واحدة» في صفحة الاستيراد (`<input multiple>`) + إجراء خادمي
  `uploadMany` يمرّ على كل ملف ويستورده في نفس الشركة، ثم يعرض ملخّصًا (مستورد/محظور/يحتاج ربطًا/فشل).

### TESTS / PASSED (بناء نظيف)
- رفع 4 ملفات معًا: ملفّا أودو (126,873 قيدًا لكل منهما) COMMITTED، ميزان نظيف COMMITTED،
  ودمج الأستاذ عبر الشهرين في نفس الشركة.
- البناء: ✓ Compiled successfully مع الإعداد الجديد.

---

## PHASE 2+++ — استقرار الذاكرة مع ملفات Excel الكبيرة

STATUS:   COMPLETE and VERIFIED

### المشكلة (توقّف الخادم / ERR_CONNECTION_REFUSED)
- قراءة ملف xlsx كبير (127k صف / 6MB) بالطريقة غير المتدفّقة كانت تبني نموذج المصنّف كاملًا
  في الذاكرة، فتستنزف الـ RAM وتُسقط عملية Node — فيظهر «رفض localhost الاتصال».

### FIXED
- **قراءة متدفّقة (streaming):** `parseXlsx` يستخدم `WorkbookReader` (صفًا صفًا) بدل تحميل
  المصنّف كاملًا — ذروة الذاكرة أقل بكثير وأسرع. (heapUsed ~105MB، rss ~251MB، القراءة 2.5s.)
- **هامش ذاكرة:** `NODE_OPTIONS=--max-old-space-size=4096` في سكربتات التشغيل (ويندوز/ماك).

### التوضيح: دعم العربية
- العربية مدعومة بالكامل — أسماء الملفات والمحتوى. اختُبر استيراد 126,873 صفًا عربيًا
  (اسم ملف عربي «دفتر الأستاذ العام 7.xlsx» ومحتوى عربي) بنجاح COMMITTED متوازن.
- سبب ظهور «رفض الاتصال» كان توقّف الخادم بسبب الذاكرة، لا العربية.

### TESTS / PASSED (بناء نظيف)
- قراءة متدفّقة: 126,873 صفًا، رؤوس صحيحة، استيراد COMMITTED متوازن (فرق 0).
- ملف مسطّح xlsx: يُقرأ صحيحًا عبر المسار العام (غير مُختطف).

---

## PHASE 3++ — تعريب رموز أنواع المخاطر في العرض

STATUS:   COMPLETE and VERIFIED

### المشكلة
- كانت بعض الشاشات تعرض رموز أنواع المخاطر الإنجليزية الخام (مثل WEEKEND_POSTING،
  TB_GL_MISMATCH) في «التقارير» و«تفاصيل الإجراء» وملف Excel — نص غير مفهوم للمستخدم.

### FIXED
- أُضيف `RISK_TYPE_LABEL` + `riskTypeLabel()` في risk.ts (تسميات عربية لكل نوع).
- استُخدم في: صفحة التقارير، صفحة تفاصيل الإجراء، وتصدير حزمة التدقيق (Excel).
- البناء: ✓ Compiled successfully.

---

## PHASE 2++++ — إصلاح تشوّه اسم الملف العربي (mojibake)

STATUS:   COMPLETE and VERIFIED

### المشكلة
- عمود «المصدر» في سجل المخاطر كان يعرض اسم الملف العربي مشوّهًا (Ø¯ÙˆØ¬Ø©…) — بايتات
  UTF-8 لاسم الملف تُقرأ كـ Latin-1 أثناء الرفع. المحتوى سليم؛ الاسم فقط تشوّه.

### FIXED
- `repairFileName()` في upload.ts: يعيد فكّ ترميز الاسم (latin1→utf8) عند اكتشاف التشوّه،
  ويترك الأسماء الصحيحة/الإنجليزية كما هي.
- يُطبَّق عند الرفع (uploadAndDetect) فتُخزَّن الأسماء صحيحة، وعند العرض (سجل المخاطر +
  قائمة الاستيراد) فتظهر البيانات المستوردة سابقًا صحيحة أيضًا.
- البناء: ✓ Compiled successfully.

---

## DEPLOY — تهيئة النشر السحابي بنقرة (auto-seed)

STATUS:   COMPLETE and VERIFIED

### IMPLEMENTED
- `ensureSeedAdmin()` (seed.ts) + `src/instrumentation.ts`: النظام ينشئ أول مدير تلقائيًا
  عند إقلاع الخادم إن لم يوجد — فالنشر السحابي يعمل بلا حاجة لصلاحية Shell لتشغيل db:seed.
- `experimental.instrumentationHook` مُفعّل في next.config.
- Dockerfile جاهز (volume على /data، DATABASE_FILE/UPLOAD_DIR مضبوطة).

### TESTS / PASSED
- تشغيل إنتاجي (`npm start`) بقاعدة بيانات جديدة بلا db:seed: السجل «seeded first admin»،
  الدخول admin@company.local → HTTP 200، كلمة مرور خاطئة → 401.
